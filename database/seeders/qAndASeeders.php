<?php

namespace Database\Seeders;

use Illuminate\Database\Seeder;
use Illuminate\Support\Facades\DB;

class qAndASeeders extends Seeder
{
    /**
     * Run the database seeds.
     *
     * @return void
     */
    public function run()
    {
        DB::statement('INSERT INTO q_and_a VALUES(
            NULL,
            "",
            1,
            NOW(),
            NOW()
        );');
        DB::statement('INSERT INTO q_and_a VALUES(
            NULL,
            "",
            1,
            NOW(),
            NOW()
        );');DB::statement('INSERT INTO q_and_a VALUES(
            NULL,
            "",
            1,
            NOW(),
            NOW()
        );');
        DB::statement('INSERT INTO q_and_a VALUES(
            NULL,
            "",
            1,
            NOW(),
            NOW()
        );');
        DB::statement('INSERT INTO q_and_a VALUES(
            NULL,
            "",
            1,
            NOW(),
            NOW()
        );');DB::statement('INSERT INTO q_and_a VALUES(
            NULL,
            "",
            1,
            NOW(),
            NOW()
        );');DB::statement('INSERT INTO q_and_a VALUES(
            NULL,
            "",
            1,
            NOW(),
            NOW()
        );');
        DB::statement('INSERT INTO q_and_a VALUES(
            NULL,
            "",
            1,
            NOW(),
            NOW()
        );');
        DB::statement('INSERT INTO q_and_a VALUES(
            NULL,
            "",
            1,
            NOW(),
            NOW()
        );');
        DB::statement('INSERT INTO q_and_a VALUES(
            NULL,
            "",
            1,
            NOW(),
            NOW()
        );');
       
        
    }
}
